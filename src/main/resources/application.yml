spring:
  application:
    name: KTUVotingapp

voting:
  adminPin: ${ADMIN_PIN:}
  userPin: ${USER_PIN:}
  # Also provide hyphen-style names because some Java @Value annotations use voting.admin-pin / voting.user-pin
  admin-pin: ${ADMIN_PIN:}
  user-pin: ${USER_PIN:}
